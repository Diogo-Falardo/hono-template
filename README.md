# Dont forger to add .env file with your database connection
**this template is using postgres but of course you can use what you want.**


##### env file
```
DATABASE_URL=
```

To install dependencies:
```sh
bun install
```

To run:
```sh
bun run dev
```

open http://localhost:3000
